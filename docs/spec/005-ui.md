# 005 — UI

Compose, one Activity, not XML Views, not a WebView of the site.

## 0.1.0 screens

| Destination | Content |
|---|---|
| Now Playing | Play/stop, volume 0–100 (default 80), title/artist, collapsible tags, progress + mm:ss / mm:ss (AFK only; live DJ hides the bar and has no track clock), listeners (centered), DJ image + `dj.djname`, **next** track then **previous** track, then thread if present |
| Songs | Section tabs **Last Played** \| **Queue**; Queue section hidden entirely when not AFK; `/r/` + blue on `type == 1` |
| Settings | Section tabs **General** \| **Auto**. General: auto-start on plug, about (r/a/dio, Geiravor, based on r/a/dio's Valkyrie, version). Auto: auto-start in vehicle |

## 0.2.0 screens

| Destination | Content |
|---|---|
| Now Playing | 0.1.0 chrome plus **Fave** (verb). Toggle the heart **immediately** on tap (fill on fave, outline on unfave) so a slow mobile IRC round-trip is not a second tap. Revert if the attempt fails or is a no-op. One in-flight fave. Failures also as red text, faded out when `np` changes |
| Songs | Section tabs **Last Played \| Queue \| Request \| Favorites**. Queue hidden when not AFK. Request is search + request. Favorites is nick + public list + per-row Request when AFK + **Request random**. Request and Favorites use the same no-scroll pane pager as news (`006-requests-faves.md`) |
| News | List + article; pane pager over a disk+RAM catalog (`007-news.md`) |
| Settings | Section tabs **General \| Auto \| Connection**. Overflow **Alerts** (alarm / snooze / sleep / DJ notifier) — not a fifth bottom tab. Connection is Rizon vs Bouncer IRC (`006-requests-faves.md`, `008-settings.md`): password and client-key fields are masked with no copy/cut; client cert may copy; Clear+confirm on each PEM; Test connection shows the server SHA-256 |

Phone: **bottom tabs** **Now Playing \| Songs \| News \| Settings** (Settings last). Two-pane only when **width ≥ 840dp and smallest width ≥ 600dp** (tablet / wide). 16:9 phones stay single-pane in landscape.

If **Now Playing** / **Last Played** do not fit one line: **two-line labels**, no hardcoded character cap. Tabs (and their icons/labels) are **vertically centered** so **Songs** sits at mid-height of **Now Playing**.

UI noun is **Favorites**. Verb / Auto command is **Fave**. Do not label anything **Faves**.

Two-pane: **Now Playing is always the left pane.** Bottom tabs are **Songs \| News \| Settings** (0.2.0) and switch only the right pane (default Songs). There is no Now Playing tab when the left pane is already showing it. Thread stays phone-only (not the tablet pane).

Songs and Settings use **section tabs** at every width.

## 0.3.0 screens

Rename the **News** bottom tab to **Board** (still four tabs). Board uses the same `SectionTabs` pattern as Songs / Settings:

**News | Schedule | Staff**

Default Board section is **News**. Phone: **Now Playing | Songs | Board | Settings**. Two-pane right: **Songs | Board | Settings**.

| Destination | Content |
|---|---|
| Board → News | Unchanged list + article (`007-news.md`) |
| Board → Schedule | Seven weekday rows, Monday first (`013-schedule-staff.md`) |
| Board → Staff | Groups Staff / Developers / DJs; image + name; no bio; Staff\|Developers paired with a separator from 600dp; DJs 4-wide / 2-wide phone (`013-schedule-staff.md`) |
| Settings → General | Theme pick Default \| Default light \| Christmas \| Halloween \| New Years; **Opt out of holiday themes**; **Convert schedule times to local**. Changing theme (or opt-out) stays on Settings; it must not send the user back to Songs / Now Playing. |

URLs, weekday order, staff role order, holiday windows, EST zone id (`America/New_York`), and Board section labels live in Policy / Rust constants, not string literals in UI.

## Layout

Follow the live homepage order for player chrome: play + volume, title, tags (+/-), progress + listeners + clock, DJ column, next, previous, then thread.

Now Playing lists the **next** song first (`queue[0]` when AFK; live DJ: `???`) and the **previous** song second (`lp[0]`). Currently playing may wrap up to two lines. Next and previous are one line each, ellipsized to the width of the screen. DJ name is centered under the image. **Listeners** is centered on its line; the AFK mm:ss clock stays on the right of that line. Full last-played and queue live on the Songs tab.

Tags: one space-separated line from `tags[]`, collapsed behind +/- like the site.

Times: relative from `timestamp` vs `current` (`002-api.md`).

Thread: phone only, **below** next/previous. Hidden on Hanyuu. Live DJ: embed `image:` / image URLs (long-press save or open); other URLs open the browser. `.gif` / `.mp4` / `.webm` on DJ image, schedule/staff cards, and thread embed autoplay; loop when that is the format default (GIF loop count, video loops). Still images stay still. Videos play muted. News article images do **not** autoplay.

## Theme: live `default-dark`

Not the old app’s orange `#DF4C3A`. Tokens from `/assets/default-dark/css/bulma.min.css`:

| Token | Live | Use |
|---|---|---|
| `--radio-secondary-1` | hsl(0, 0%, 11%) | screen background |
| `--radio-primary-1` | hsl(0, 0%, 13%) | cards / bars |
| `--radio-primary-3` | hsl(0, 0%, 24%) | borders / raised |
| `--text-secondary` | hsl(0, 0%, 96%) | primary text |
| `--text-primary` | hsl(0, 0%, 50%) | muted text |
| `--blue` | hsl(208, 27%, 39%) | play / request / links |
| `--red` | hsl(348, 27%, 50%) | errors |
| `--green` | hsl(153, 27%, 39%) | success |
| `--text-link` | hsl(208, 27%, 50%) | links |

Play control uses the blue (`is-info` equivalent). Material 3 is re-skinned to these tokens.

## 0.3.0 theme packs

`RadioTheme` is the **current pack**, not loose HSL in call sites. Packs are committed Compose tokens. Copy/update them by hand from live `/assets/{theme}/css/` (and `main.css` + images) when a pack is added or the site moves. Do **not** download or parse CSS at runtime. Do **not** WebView the site.

Holiday **wallpapers** are committed under `drawable-nodpi/` (`wallpaper_{christmas,halloween,newyears}.jpg`), scaled from live `/assets/{theme}/images/`. Phone only; Auto never paints them.

Live holiday CSS shares one bulma token file; packs still differ:

| Pack | From live | Surface | Play/accent (`--edenlight-color`) | Wallpaper |
|---|---|---|---|---|
| `default-dark` | table above | dark cards | hsl(208, 27%, 39%) | none |
| `default-light` | stock Bulma light (no `--radio-*`): scheme 221/14%, text 21%/48%, info 198, link 233 | off-white page, white cards | hsl(198, 100%, 41%) | none |
| `christmas` | edenlight + `#38A8E4` | **white** cards, dark card text, white page text | `#38A8E4` | `christmas.jpg` |
| `halloween` | edenlight + `#F4A246` + dark glass | **~80% black** cards, white text | `#F4A246` | `halloween.png` |
| `newyears` | edenlight + `#60709f` + dark glass | **~80% black** cards, white text | `#60709f` | `newyears.webp`/`png` |

Christmas snow and Halloween hair/moon/pumpkins are light; uncarded lists (Request, Favorites, news body, staff, schedule) sit on a **pane** (white sheet / glass frost) so text is not painted on the wallpaper. Glass scrim is opaque enough that light wallpaper regions do not wash the type.

Play, volume, and request keep that pack’s `--edenlight-color`. Selection chrome (theme radios, pager current, tab/nav indicator, switches, schedule today) uses **highlight**: the same hue, lifted on glass packs until it reads on wallpaper (New Years `#60709f` especially). Selection wash is stronger on white Christmas cards and on glass. Non-glass packs keep the accent hue as-is.

Do **not** invert default-dark to make default-light. Do **not** collapse holidays to one palette.

| Pack | When |
|---|---|
| `default-dark` | User default (current 0.2.0 tokens) |
| `default-light` | User picker |
| `christmas` / `halloween` / `newyears` | User picker **any day**, and auto when the **live site** is serving that theme |

Settings → General: Theme **Default** | **Default light** | **Christmas** | **Halloween** | **New Years** (DataStore). **Opt out of holiday themes** (default **off** = auto allowed) only blocks the **automatic** switch; a manual holiday pick still applies. Same idea as the site cookie `OverrideHoliday` for auto.

Holiday detection: the site turns holidays on when an admin sets `HolidayTheme`, not by calendar. Geiravor only **looks** during these **device-local** windows:

| Pack | Window (inclusive) |
|---|---|
| Halloween | 29 October – 1 November |
| Christmas | 1–26 December |
| New Years | 27 December – 3 January |

Outside those windows: never sniff, never auto-apply a holiday pack. Inside: sniff on **process start**. If the site is **not** on that holiday yet, at most **once per hour** until it is. **Once seen on** (`christmas` / `halloween` / `newyears` on the HTML), **stop sniffing until that window ends**. App start may sniff again. Reuse HTML already in hand (news/schedule/staff) before `GET /`. Not every `/api` poll.

If the public theme is one of the three holidays and the user has **not** opted out, apply that pack (overrides the user pick until the window ends). Otherwise use the user pick, which may itself be a holiday pack. Do **not** follow `dj.theme_id` / `dj.css`. Other public names (`suzu`, `tuicss`, `edenlight`) are ignored. Holiday packs (manual or auto) are **phone-only**; Auto follows Default / Default light (a holiday user pick or holiday auto maps to Default). Application night mode is Default light → day, everything else → night (`004-android-auto.md`).

## Assets (committed)

From `https://r-a-d.io/assets/images/` (and mystery-DJ from the site’s `mystery-dj.png` if present, else a committed placeholder):

- `logo_image_small`
- `logotitle_2`
- launcher mipmaps
- mystery-DJ fallback — site `mystery-dj.png`; committed copy `drawable-nodpi/mystery_dj.jpg`
- holiday wallpapers — `drawable-nodpi/wallpaper_{christmas,halloween,newyears}.jpg` from live `/assets/{theme}/images/` (scaled; do not fetch at runtime)

Gradle must not read files outside this repo.

Launcher label: `r/a/dio`.

## Edge-to-edge

Use compat (`009-compat.md`). Content draws behind system bars with insets padding.
