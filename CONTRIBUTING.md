# Contributing

How to work in this tree is [`AGENTS.md`](AGENTS.md). Product is [`docs/spec/`](docs/spec/). Repo contract is [`docs/`](docs/README.md).

Issues: **bug** (code disagrees with the spec) or **spec** (change the contract). Pull requests: Conventional Commit title, one logical change, GPG-signed commits. Land on `main` through a PR. CI is [`.github/workflows/ci.yml`](.github/workflows/ci.yml).
